# Welcome to DeerCode

A coding agent in your terminal.

- Type your request in the chat box on the left.
- Watch file edits appear in the editor, and commands in the terminal.
- Type `exit` or `quit` (or press `q`) to leave.
